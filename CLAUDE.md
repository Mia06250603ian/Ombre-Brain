# 这个仓库是什么(给每个新会话的 Claude 看,开场自动读到)

**一句话:这里躺着五个互相独立的服务,原本这仓库只是 OB 的。**

2026-07 建 kelivo-shim 时图省事没另开仓库,后面接的服务也就顺手放了进来。
**没拆是刻意的决定(2026-08-21 所有者拍板),不是没人管** —— 别手痒去整理,
理由和「万一要拆怎么拆」都写在本文最后一节。

## 地图:哪个目录是什么

| 目录 | 是什么 | 源码在这儿? | 它的手册 |
|---|---|---|---|
| **根目录** | **OB 记忆库**(Python/FastMCP)。**本仓库的主人**:Zeabur 从 main 拉、每日备份和 CI 都挂在它身上 | ✅ | `INTERNALS.md`(内部机制)、`README.md`(对外的安装说明)、`ENV_VARS.md`、`BEHAVIOR_SPEC.md` |
| `kelivo-shim/` | **晏的本体**:维护一个常驻 `claude -p` 进程,人设/记忆工具/上下文守卫都在这 | ✅ | `kelivo-shim/MAINTENANCE.md`(**开头有目录,按目录取用**) |
| `telegram-bridge/` | Telegram ⇄ shim 的桥(去抖/语音/贴纸/推送/查岗) | ✅ | `telegram-bridge/MAINTENANCE.md`(同上,有目录) |
| `gmail-mcp/` | 晏的邮箱(读信/搜信/写草稿,发信白名单制) | ✅ | `gmail-mcp/MAINTENANCE.md` |
| `dwell-bridge/` | 自建网页 ⇄ shim 的转接层 | ✅ | `dwell-bridge/MAINTENANCE.md` |
| `browser-hands/` | 晏的「浏览器的手」 | ❌ **只有手册**,源码在 `Mia06250603ian/browser-hands` | `browser-hands/MAINTENANCE.md` |

**五个服务的代码零跨目录依赖**(2026-08-21 实测:没有任何一个 `../` 的 import)。
一个目录 = 一个独立服务,各跑各的、各有各的 Zeabur 服务、各看各的手册。

## 干活前读什么

1. **`OPERATIONS.md` 读到第 4 节**(约 1 万 token)—— 全局拓扑、服务清单、规矩。够开工了。
   第 5 节起是速查和故障表,**按症状对号再看**;第 6 节是三个服务的上线流程;第 9 节是写文档的规矩。
2. **要动哪个服务**:先看那份 `MAINTENANCE.md` 开头的《怎么读这份手册》目录,按它取用;
   **但「踩坑」那节必须全文读完,一条都别跳**(真事故换来的,只扫加粗标题不算读过)。
   没写目录的手册(gmail / dwell / browser)照旧全文读完,它们本来也只有 1 万 token 上下。
3. **改 OB 记忆库** → `INTERNALS.md`;**动 shim 之前** → 必读它的《部署检查单》。
4. **历史不用开场读**:功能时间线在 `TIMELINE.md`(按日期搜),
   shim 的部署记录在 `kelivo-shim/DEPLOY-LOG.md`(**33 条,覆盖第 2~34 次**;第一次记在 `TIMELINE.md` 07-12,按「第 N 次」搜)。

## 三条铁规矩(违反过,都出过事)

1. **所有者来问问题 ≠ 授权你动手。** 诊断随便做;**任何改动、部署、以她名义发消息,先说明、等她点头。**
2. **仓库最新代码是唯一可信源。** 动手前 `git pull`,部署前和线上容器 md5 对账,
   严禁拿会话里的旧副本部署(2026-07-13 出过事,把线上滚回了旧版)。
3. **部署 shim 前,让所有者本人对晏说「归档」**(除非她明确说不用)。重启会清掉晏当前窗口的上下文,
   **代她发这句话也不行**(踩坑 13:晏会起疑、可能拒绝执行,窗口照样丢)。

**改完东西要更新文档,照 `OPERATIONS.md` 第 9 节《写手册的规矩》写**(那节动笔前再读,平时不用读)。

## 不在仓库里的东西(别找,也别往里放)

- **密钥值全在 Zeabur 环境变量里**,一律不入库。
- **人设文件**(`ian.md` / `profile-instructions.md`)和 `mcp-servers.json` **刻意不在仓库**,
  取法见 `kelivo-shim/MAINTENANCE.md`「本目录刻意缺的三个文件」。

## 万一以后不得不拆

**别现场从头研究** —— `OPERATIONS.md` 第 10 节《万一以后不得不拆:照这个做》里有现成清单
(每个服务搬走要干什么、要改多少条指路、哪个不能搬、为什么现在不拆)。**动这件事之前先读那节。**
