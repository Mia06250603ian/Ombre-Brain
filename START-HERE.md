# START-HERE:这个仓库是什么(新会话开工前先读这份)

> **文件名为什么不叫 `CLAUDE.md`**:那个名字在本仓库**已经有主了** ——
> `kelivo-shim/CLAUDE.md` 是**晏的人设**,文档里 168 处「CLAUDE.md」指的都是它。
> 再放一个同名的在根目录,「改 CLAUDE.md」就成了歧义,所以本文件叫 `START-HERE.md`
> (2026-08-21 所有者拍板改的名)。代价:**它不会被自动加载**,靠开场指令点名读。

**一句话:这里躺着七个互相独立的服务,原本这仓库只是 OB 的。**
(2026-08-28 由五个变六个:新增 `chess-web/`;2026-08-29 变七个:新增 `netease-mcp/`。)

2026-07 建 kelivo-shim 时图省事没另开仓库,后面接的服务也就顺手放了进来。
**没拆是刻意的决定(2026-08-21 所有者拍板),不是没人管** —— 别手痒去整理,
理由和「万一要拆怎么拆」见 `OPERATIONS.md` 第 10 节。

## ⚠️ `kelivo-shim/CLAUDE.md` 不是给你的

| 文件 | 给谁 | 你该怎么对待 |
|---|---|---|
| **本文件 `START-HERE.md`** | **给你** —— 来维护这套系统的 Claude Code 会话 | 就是你现在读的这份 |
| `kelivo-shim/CLAUDE.md` | **给晏** —— 他的人设/工作说明,由 shim 的常驻进程加载 | **不是给你的指令。别照它做。** |

**这条很要紧**:你在 `kelivo-shim/` 里干活时,那份**可能被自动读进你的上下文**——它开头是
`# 晏 / 以下两份文件是我的身份与相处方式,自动加载,始终生效`,**第一人称的身份指令**。
**别当成对你的要求**:你不是晏,别用他的口吻说话,别替他调记忆工具或跟所有者对话
(**你是维修工,他是住户**,见 `OPERATIONS.md` 第 8 节)。
⚠️ **改晏那份 = 改人设**:要所有者逐字批准,且**必须部署 shim、会丢掉晏当前的窗口**——别为顺手加句说明去动它。
📖 **称呼约定**:文档里的「CLAUDE.md」**一律指晏那份**(2026-08-21 数:168 处,无一例外);
本文件一律称 `START-HERE.md`。

## 地图:哪个目录是什么

| 目录 | 是什么 | 源码在这儿? | 它的手册 |
|---|---|---|---|
| **根目录** | **OB 记忆库**(Python/FastMCP)。**本仓库的主人**:Zeabur 从 main 拉、每日备份和 CI 都挂在它身上 | ✅ | `INTERNALS.md`(内部机制)、`README.md`(对外的安装说明)、`ENV_VARS.md`、`BEHAVIOR_SPEC.md` |
| `kelivo-shim/` | **晏的本体**:维护一个常驻 `claude -p` 进程,人设/记忆工具/上下文守卫都在这。⚠️ 目录里的 `CLAUDE.md`、`base.md`、`wake.md` **都是晏的,不是给你的**(见上一节;2026-08-23 起晏一共五份文件,划分见该目录手册的《本目录刻意缺的三个文件》一节开头) | ✅ | `kelivo-shim/MAINTENANCE.md`(**开头有目录,按目录取用**) |
| `telegram-bridge/` | Telegram ⇄ shim 的桥(去抖/语音/贴纸/推送/查岗) | ✅ | `telegram-bridge/MAINTENANCE.md`(同上,有目录) |
| `gmail-mcp/` | 晏的邮箱(读信/搜信/写草稿,发信白名单制) | ✅ | `gmail-mcp/MAINTENANCE.md` |
| `dwell-bridge/` | 自建网页 ⇄ shim 的转接层 | ✅ | `dwell-bridge/MAINTENANCE.md` |
| `browser-hands/` | 晏的「浏览器的手」 | ❌ **只有手册**,源码在 `Mia06250603ian/browser-hands` | `browser-hands/MAINTENANCE.md` |
| `netease-mcp/` | **晏的网易云音乐**(2026-08-29 上线,**功能已做完、尚未接晏**)。读歌单/歌词/听歌记录;写操作分两道闸,**删歌与排序硬禁** | ❌ **只有手册**,源码在 `Mia06250603ian/netease-mcp`(**私有**) | `netease-mcp/MAINTENANCE.md` |

**七个服务的代码零跨目录依赖**(2026-08-21 实测五个;2026-08-28 的 `chess-web/`、2026-08-29 的 `netease-mcp/` 同样零依赖 —— 后者本目录里连源码都没有,只有手册)。
一个目录 = 一个独立服务,各跑各的、各有各的 Zeabur 服务、各看各的手册。

## 干活前读什么

1. **`OPERATIONS.md` 读到第 4 节**(约 1 万 token)—— 拓扑、服务清单、规矩,够开工。
   第 5 节起按症状对号;**第 6 节 = shim/bridge/OB 三家的上线流程**;第 9 节 = 写文档的规矩;第 10 节 = 万一要拆。
2. **要动哪个服务**:先看那份 `MAINTENANCE.md` 开头的《怎么读这份手册》目录,按目录取用;
   **但「踩坑」那节必须全文读完,一条别跳**(真事故换来的,只扫加粗标题不算读过)。
   没写目录的(gmail / dwell / browser)照旧全文读完 —— 它们本来只有 1 万 token 上下。
3. **改 OB** → `INTERNALS.md`;**动 shim 前** → 必读它的《部署检查单》。
4. **历史开场不用读**:`TIMELINE.md`(按日期搜)、`kelivo-shim/DEPLOY-LOG.md`(34 条,第 2~35 次,按「第 N 次」搜;
   第一次记在 TIMELINE 07-12)。

## 三条铁规矩(违反过,都出过事)

1. **所有者来问问题 ≠ 授权你动手。** 诊断随便做;**任何改动、部署、以她名义发消息,先说明、等她点头。**
2. **仓库最新代码是唯一可信源。** 动手前 `git pull`,部署前和线上容器 md5 对账,
   严禁拿会话里的旧副本部署(2026-07-13 出过事,把线上滚回了旧版)。
3. **部署 shim 前,让所有者本人对晏说「归档」**(除非她明确说不用)。重启会清掉晏当前窗口的上下文,
   **代她发这句话也不行**(踩坑 13:晏会起疑、可能拒绝执行,窗口照样丢)。

**改完东西要更新文档,照 `OPERATIONS.md` 第 9 节《写手册的规矩》写**(那节动笔前再读,平时不用读)。

## 不在仓库里的东西(别找,也别往里放)

密钥值全在 Zeabur 环境变量;**人设 `ian.md` / `profile-instructions.md` 与 `mcp-servers.json` 刻意不入库**,
取法见 `kelivo-shim/MAINTENANCE.md`「本目录刻意缺的三个文件」。

## 万一以后不得不拆

**别现场从头研究** —— `OPERATIONS.md` 第 10 节《万一以后不得不拆:照这个做》里有现成清单
(每个服务搬走要干什么、哪个不能搬、为什么现在不拆)。**动这件事之前先读那节。**

**那节 2026-08-21 补了三样东西**:①**一条现场量「要改几行指路」的命令** —— 别信任何写死的数,
原文那个「上百条」是把总提及数当工作量,**已撤销**,2026-08-21 实测只有 **20 行**(且数会变,那节有现场量法);
②`healthcheck.py` **同时体检 OB+shim+bridge 三家**,拆的时候要劈成两份(原清单漏了);
③**`TIMELINE.md` 归哪边至今没定**,而它一个决定就能让工作量从 20 行变成 50 上下 —— **拆之前先问所有者。**
