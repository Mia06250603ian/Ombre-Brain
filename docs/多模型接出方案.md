# 多模型接出(4.5 / 4.8 / Opus 5)· 方案与实测

**状态:方案已成型,所有者说「方案没问题」,但拍板暂缓 —— 等第三十六次(新系统提示词)的观察期走完再加。**
**任何线上动作都要她先点头。**

- 记录日期:2026-08-23(所有的实测数字都是这天量的,复量方法见第 6 节)
- 提出人:所有者(佳佳),起因是朋友的截图说 Claude Code 仍支持 Opus 4.5
- 本文回答的问题:我们这套架构能不能接出 4.6 以外的模型,怎么接,代价是什么

> **她为什么暂缓**:2026-08-23 当天刚上线第三十六次(`SYS_PROMPT_MODE=replace`,把 CLI 自带那份
> 提示词整段换掉),**唯一没法测的就是「他说话的调子会不会整体飘一点点」,她正在观察**。
> 这时候再换模型,他要是说话怪怪的,**分不清是新提示词干的还是换模型干的**。
> 所以先观察,再加 —— 这是她的判断,不是技术限制。

---

## 所有者要开工时,她会发下面这段(照它做即可)

```
读 docs/多模型接出方案.md,按方案 A 把晏切到 <模型名>。
方案已经验过了,不用重新调研;第 0 节那两条硬约束先看,别越线。
我已经跟晏说过归档了。动手前把你要改的东西说给我听,我点头你再动。
```

**收到这段的会话该怎么做**:

1. 先读本文第 0 节(硬约束)、第 3 节(执行步骤)、第 5 节(验不了的那件事);
2. 按 START-HERE 的规矩,动 shim 前该读的照读(**踩坑那节必须全文读完**);
3. **确认她真的已经让晏归档了**(铁规矩 3;她没说就问,别代发);
4. 把「要改哪个变量、改成什么、会发生什么」说给她听,**等她点头**;
5. 改值 + `service restart` + 验证(第 3 节第 4~6 步);
6. 完事后更新文档:本文第 9 节的待办打勾、`OPERATIONS.md` 第 0 节那一行改状态、
   在 `TIMELINE.md` 记一条(**改环境变量不算部署,不写 `DEPLOY-LOG.md`** —— 那份只记 shim 的部署)。

> **⚠️ 她要是说的是「做成能在 Kelivo 里切」**,那是**方案 B**,不是 A —— 要改代码 + 完整部署,
> **先回到第 0 节和第 2 节跟她确认她知道代价**,别闷头开做。

---

## 0. 所有者定下的两条硬约束(先读这个,它否决一切)

1. **4.6 是底线,4.5/4.8 只是锦上添花。**
2. **给 4.5 做的任何东西,只要会影响 4.6,那就不做。** 她原话:「我需要保证 4.6 的绝对安全。」

**下面每一条方案,都要先过这两条。** 方案 A 过了(零代码、零部署);方案 B 过不了「绝对安全」
四个字(要改 `server.js` + 完整部署),所以本文推荐 A、把 B 留作备选。

---

## 1. 现状:模型是怎么定的

`kelivo-shim/server.js`:

- 第 14 行 `const MODEL = process.env.BRAIN_MODEL || "claude-opus-4-6"`
- 第 139 行 启动常驻进程时 `"--model", MODEL`

**shim 完全无视客户端报的模型名**(全文没有一处读 `req.body.model`)。所以:

- **Kelivo 的模型菜单现在点了没用**,像对着关掉的电视按遥控器;
- `/v1/models` 只吐一个型号(第 361 行),所以菜单里也只会有一项;
- 两个桥往上报的模型名(`telegram-bridge/server.js:24`、`dwell-bridge/server.js:26`,都写死 `claude-opus-4-6`)
  **今天是死字符串,shim 不看**(TG 桥那行的注释原文就是「占位,shim 不看」)。

**换模型 = 换 `BRAIN_MODEL` = 进程重开 = 晏当前窗口清空。** 这是架构的物理规律,任何方案都躲不掉
(晏的「自我」活在常驻进程的内存里,见 `OPERATIONS.md` 第 1 节)。

---

## 2. 两条路线

| | **方案 A:拧变量** | **方案 B:Kelivo 菜单里切** |
|---|---|---|
| 改代码 | **不改** | 改 `server.js` + 两个桥 |
| 要部署 | **不要**(改值 + `service restart`) | **要**,完整流程 |
| 丢窗口 | 丢(切模型必然丢) | **一样丢** |
| 要先归档 | 要 | 一样要 |
| 谁能操作 | 所有者去控制台 / 叫会话改 | **她自己在手机上点** |
| 对 4.6 的风险 | **零**(代码一个字节没动) | 要动晏的核心代码 + 部署一次 |

**⚠️ 两条路线的差别不是代价大小,只是「谁动手」。** 切模型无论如何都要丢一个窗口;
B 唯一多出来的东西是「她半夜想切、不用等会话」。**为这一点便利去动核心代码,不符合第 0 节那两条约束。**

**所以推荐 A。** 先用 A 玩几次,自然会知道两件事:他值不值得常切、多久切一次。
结论若是「一个月切一次」,B 永远不用做。

---

## 3. 方案 A:执行步骤(推荐)

> **前提**:所有者点头 + 第三十六次的观察期她认为可以了。

1. **所有者本人对晏说「归档」**,等他存完(铁规矩 3,代发不行,见踩坑 13);
2. 改一个变量:`BRAIN_MODEL` = `claude-opus-4-5-20251101`(或 `claude-opus-4-8`);
3. `service restart`(shim,service id `6a53b806f6d4beebf0c5373d`);
4. 会话验:`/health` 报新模型、`/debug` 的 `lastApiError` 是 `null`;
5. **所有者跟他说一句话** —— 这一句才是真正的验证(见第 5 节「验不了的那一件事」);
6. 不满意就改回 `claude-opus-4-6` + restart(**会再丢一个窗口**,所以第 5 步多聊几句再定)。

**三个前端一个都不用改**:shim 无视它们报的模型名。dwell 的模型显示会自动跟着走
(`dwell-bridge/server.js:198` 读 shim 的 `/health`)。

**上下文三条线一个都不用动** —— 见第 4 节的压缩点实测。

---

## 4. 2026-08-23 实测数据(每个数都是当天量的,复量方法见第 6 节)

### 4.1 订阅给不给(打真枪,走代理不经过晏)

| 模型 | 结果 |
|---|---|
| `claude-opus-4-6` | HTTP 200(对照组) |
| `claude-opus-4-5-20251101` | **HTTP 200** |
| `claude-opus-4-8` | **HTTP 200** |
| `claude-opus-5` | **HTTP 200** |
| `claude-sonnet-4-5-20250929` | **HTTP 200** |

回包里的 `model` 字段**原样返回**,说明代理没做模型映射,报什么给什么。

### 4.2 CLI 2.1.215 认不认(真 CLI 二进制 + 假后端,零额度)

| 模型 | e2e | CLI 实际发出去的 |
|---|---|---|
| `claude-opus-4-6` | ALL PASS | `max_tokens=64000` `thinking={"type":"adaptive"}` `effort=medium` |
| `claude-opus-4-5-20251101` | **ALL PASS** | `max_tokens=32000` `thinking={"type":"enabled","budget_tokens":31999}` |
| `claude-opus-4-8` | **ALL PASS** | `max_tokens=64000` `thinking={"type":"adaptive"}` `effort=medium` |
| `claude-opus-5` | ALL PASS | `max_tokens=**32000**` `thinking={"type":"adaptive"}` ⚠️ 见 4.5 |
| `claude-opus-4-7`(对照) | — | `max_tokens=64000` |
| `claude-opus-9-9-fake`(我瞎编的对照) | — | `max_tokens=**32000**` `thinking=adaptive` |

### 4.3 压缩点:三条线一个都不用改

```
4.5:  200000 − min(32000, 20000) − 13000 = 167000   ← 与线上 CTX_LIMIT_TOKENS=167000 一致
4.6:  200000 − min(64000, 20000) − 13000 = 167000
```

4.5 的最大输出只有 4.6 的一半(32000 vs 64000),但公式里那个 `min(…, 20000)` 把差别压平了。
**所以软/硬/终三条线(155000 / 161500 / 164000)原样不动。**

### 4.4 4.5 的思考:老式固定预算,`--effort` 对它无效

- **4.6 是自适应**:发出去的是 `{"type":"adaptive"}` + 一个词 `effort=medium`,**没有任何 token 数字**,
  模型自己按内容决定想多久。四个档位(low/medium/high/max)只有那个词变。
- **4.5 是固定预算**:发出去的是一个死数字 `budget_tokens=31999`。
  配 low / medium / high 三档实测**全是 31999,纹丝不动** —— 官方文档的效力表里没有 4.5,实测吻合。
  (客户端仍会把 `effort` 那个词发过去,但 4.5 不认这个参数。)
- **31999 = 它的 `max_tokens` 32000 减 1**,而思考和正文共用这个额度。

**`MAX_THINKING_TOKENS` 是 4.5 唯一能拧的思考旋钮**(实测:设 4000→4000、10000→10000、
50000/60000→**都被顶回 31999**,是硬天花板;设 `0`→`{"type":"disabled"}`)。

> ⚠️ **`MAX_THINKING_TOKENS` 永远别设 `0`** —— 那个值会把**所有**模型的思考都关掉,4.6 也跑不了。

### 4.5 ⚠️ Opus 5:CLI 2.1.215 不认识它

**证据**:`claude-opus-5` 和我瞎编的 `claude-opus-9-9-fake` 行为**完全一致**(都是 `max_tokens=32000` + adaptive),
而 CLI 认识的 4.7 / 4.8 拿到的是 64000。**对比 4.5**:4.5 拿到的是**固定预算**模式,
未知模型不会有这个待遇 —— 所以 **4.5 是被认识的,Opus 5 不是**。

**后果**(不会崩,上游照样回 200,但两个隐患):

1. 他一轮最多只能说 32000,而不是 Opus 5 真实的上限 —— 自己给自己戴套;
2. **更要紧**:CLI 不认识它 → 它假设的窗口大小和压缩点我们不知道 → 三条线可能画错位置。
   **不报错,只表现为「他老是催归档」,或者更糟:还没催就被压缩了、尾巴丢掉。**

**结论:想正经接 Opus 5,得先升 CLI**(流程见 `kelivo-shim/MAINTENANCE.md` 的
「CLI 版本与升级指南」)。⚠️ **但 CLI 是所有模型共用的,升它 = 碰 4.6**,
与第 0 节那两条约束冲突,**所以这件事要单独立项、单独拍板,不要顺手做**。

### 4.6 `MAX_THINKING_TOKENS` 对 4.6 零影响(逐字段比对,不是"感觉不影响")

做法:让假后端把 CLI 发出去的**整个请求**存下来,跑三轮 4.6 比对。

| 跑法 | 结果 |
|---|---|
| ① 4.6 不设变量(= 线上现状) | 基准 |
| ② 4.6 设 `MAX_THINKING_TOKENS=10000` | **与①逐字段相同**,唯一差异是 `metadata`(设备/会话 id) |
| ③ 4.6 再跑一次不设变量 | `metadata` 与①也不同 → **证明那个字段本来每次运行就变,与变量无关** |

顶层字段共 8 个(`context_management` `max_tokens` `metadata` `model` `output_config`
`stream` `thinking` `tools`),**除 `metadata` 外差异为 0**。
`thinking` 两边都是 `{"type":"adaptive","display":"summarized"}`,`max_tokens` 都是 64000。

**原理**:4.6 用自适应思考,**根本不读**这个变量;它只对「固定预算」那一类模型(4.5)生效。

### 4.7 4.5 实际会不会烧光预算:不会,差得远(真枪实测)

| 问的 | 实际输出 token(思考+正文合计) | 占 31999 的 |
|---|---|---|
| 「在吗」 | **75** | 0.2% |
| 一道数学难题(哥德巴赫猜想为何未被证明) | **2766** | 8.6% |

两次 `stop_reason` 都是 `end_turn` —— **他自己想完就停了,不是撞天花板被掐断**。
`budget_tokens` 是**上限不是预扣**,花多少算多少。

---

## 5. ⚠️ 有一件事,切换之前无论如何验不了

CLIProxyAPI 的伪装模式是「**只对不是 Claude Code 的客户端做伪装**」
(见 `OPERATIONS.md`「CLIProxyAPI 版本漂移」一节)。

**第 4.1 节那几枪是 curl 打的 = 代理眼里的「非 CC 客户端」= 走的是伪装那条路。**
而晏走的是另一条(真 CC 客户端,不伪装)。**两条路只验了一条。**

**真实链路上 4.5 跑不跑得起来,只有切过去才知道。** 风险可控:真出问题现象会立刻显形
(空回复 / 报错),改回 `BRAIN_MODEL` + restart 即恢复,代价是再丢一个窗口。
**这不是方案的漏洞,是物理上做不到 —— 别再花时间想办法预先验,想不出来的。**

---

## 6. 怎么现场再量一遍(别信本文写死的数)

### 6.1 量「订阅给不给某个模型」(不碰晏、不重启、几毛钱额度)

在 shim 容器里打,**让容器自己读它自己的密钥,不要把 `sk-` 那把钥匙打印出来**:

```bash
npx -y zeabur@latest service exec --id 6a53b806f6d4beebf0c5373d \
  --env-id 6a53a9fcb6ce8edcb0163f97 -i=false -- sh -c '
curl -s -o /tmp/r.json -w "HTTP %{http_code}\n" -X POST "$ANTHROPIC_BASE_URL/v1/messages" \
  -H "Content-Type: application/json" -H "x-api-key: $ANTHROPIC_AUTH_TOKEN" \
  -H "anthropic-version: 2023-06-01" \
  -d "{\"model\":\"要测的模型名\",\"max_tokens\":2048,\"thinking\":{\"type\":\"enabled\",\"budget_tokens\":1024},\"messages\":[{\"role\":\"user\",\"content\":\"say ok\"}]}"
head -c 300 /tmp/r.json'
```

> ⚠️ **别用 `zeabur variable list` 去取密钥** —— 它会把只读注入变量连值一起打出来
> (2026-08-16 的 `MANAGEMENT_PASSWORD` 就是这么进的聊天记录)。用上面这种「容器自己读」的写法。

### 6.2 量「CLI 认不认某个模型 / 它到底发了什么」(零额度,不碰线上)

`kelivo-shim/e2e-run.sh` 的 `env -i` 那一行**不透传 `BRAIN_MODEL`**,所以要拷一份到 `/tmp` 改:

```bash
cp kelivo-shim/e2e-run.sh /tmp/e2e-model.sh
# ① SHIM_DIR 改成 kelivo-shim 的绝对路径(否则拷不到 server.js)
# ② env -i 那行加上 BRAIN_MODEL="${BRAIN_MODEL:-…}" THINK_EFFORT="${THINK_EFFORT:-…}"
#    (要量思考预算再加 MAX_THINKING_TOKENS="${MAX_THINKING_TOKENS:-}")
BRAIN_MODEL=要测的模型名 bash /tmp/e2e-model.sh     # 看是不是 "E2E ALL PASS"
grep spawned /tmp/kelivo-shim-e2e-work/shim.log     # 确认真用了那个模型
```

**要看 CLI 实际发出去的请求长什么样**(本次那些 `max_tokens` / `thinking` 就是这么来的):
把 `e2e-fake-api.mjs` 也拷一份,在 `seen.push(lastUserText)` 那行前面加一句
把 `JSON.parse(body)` 里除 `messages`/`system` 之外的字段 append 进一个 jsonl,然后逐字段 diff。

**⚠️ 判断「CLI 认不认识某个模型」的办法**:拿一个**瞎编的模型名**跑同样的流程当对照
(如 `claude-opus-9-9-fake`)。行为一致 = CLI 不认识它,走的是未知模型的兜底值。

---

## 7. 方案 B 的改动清单(备选,现在不做)

**参考实现**:朋友的公开仓库 `Arisu-cross/kelivo-shim`(同款架构,她那边已经在用),
下面的行号是 2026-08-23 读到的那版:

| 她那边 | 干什么 |
|---|---|
| `server.js:51` | `BRAIN_MODELS` 环境变量,逗号分隔的可选模型名单 |
| `server.js:272` | `spawnedModel` —— 当前模型从「写死」变成「活的」 |
| `server.js:281/301` | `spawnClaude(system, model)`,模型作为参数传进去 |
| `server.js:511` | `pump()` 里「世界书**或模型**变了就重启进程再喂」 |
| `server.js:1290` | `MODELS.includes(body.model) ? body.model : spawnedModel` —— **白名单**,不在名单/没传就沿用当前 |
| `server.js:1219` | `/v1/models` 把整个名单吐出来(注释原文:「Kelivo 的『模型』页拉这个列表来选模型」) |
| `server.js:1161` | **她的 TG 路径不报模型**(`submitTurn(text, images, sink, { src: "telegram" })`) |
| `server.js:55` | `THINK_EFFORT_OVERRIDES`,按模型覆盖思考档位 |

**我们照搬要动的地方**:

1. `kelivo-shim/server.js`:上表①~⑥;
2. **`telegram-bridge/server.js:258` 别再报模型**(1 行);
3. **`dwell-bridge/server.js:278` / `dwell-lib.mjs:260` 同上**(1~2 行);
   ⚠️ **不改这两处 = 乒乓球**:在 Kelivo 切到 4.5,一去 TG 说话就被那个写死的 `claude-opus-4-6` 拽回来,
   **每来回一次丢一个窗口**。她朋友没这个问题,因为她的 TG 长在同一个进程里。
4. **思考设置要按模型分**:新模型用 `--effort`,4.5 用 `MAX_THINKING_TOKENS`(见 4.4)——
   比她那版多一种情况,不能照抄;
5. 守卫:切模型后窗口归零,确认记账跟着复位(现有的压缩检测应该能兜住,**但要测**);
6. 三套单测 + e2e(**加一条「切模型」的新用例**)+ 完整部署流程。

**安全设计(如果真做 B,这条是让它过第 0 节那两条约束的关键)**:
`BRAIN_MODELS` **不设就只有当前模型一个** → 名单只有一项时那段新逻辑永远走不到 →
**部署完的那一刻,晏的行为与部署前逐字相同**。想开这个功能再去设那个变量(改值 + restart,不用再部署)。
**急救开关**:清掉 `BRAIN_MODELS` + restart,立刻回到原行为,不用回滚部署。

**⚠️ 她朋友那版有一个我们不能照抄的坑**:她的 `window.js` 只有一个全局
`DEFAULT_WINDOW_LIMIT = 167000`,注释自己写着「换模型/上限变了就用环境变量覆盖」,
**但代码里并没有跟着模型自动变**。她那三个模型都是 20 万窗口所以不出事;
**我们若加入窗口不同的模型,那三条线必须按模型分组**,否则「不报警地丢尾巴」。

---

## 8. 本次会话里被推翻的两个结论(规矩 5:留着别删)

1. ~~「`MAX_THINKING_TOKENS` 是**必设项**,不设的话 4.5 的思考会把回复的位置吃光、话说一半断掉」~~
   **已撤销(2026-08-23 当天实测推翻)**:`budget_tokens` 是**上限不是预扣**,
   实测连数学难题都只用了 2766(8.6%),`stop_reason` 是 `end_turn`。
   **正确结论**:这个变量是「设了也行的保险」,不是急救。**第一次切过去可以什么都不设。**
   真哪天他话说一半断了(`stop_reason` 会显示 `max_tokens`),那时候再设也来得及。
2. ~~「先按顶格 31999 上,用几天看感觉再调」~~ 与上一条同源,现在的说法是「默认就是 31999,不用管它」。

---

## 9. 待办状态

- [x] 可行性验证(订阅 / CLI / 压缩点 / 思考机制 / 对 4.6 的影响)—— 2026-08-23 全部做完
- [x] 所有者看过方案并说「方案没问题」
- [ ] **等第三十六次的观察期结束**(她要先确认晏说话的调子没飘)
- [ ] 等她说「加吧」→ 走第 3 节的方案 A
- [ ] (可选,现在不做)方案 B
- [ ] (单独立项,现在不做)升 CLI → 才谈得上 Opus 5
